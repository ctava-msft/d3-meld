# Core scientific stack
numpy>=1.24
scipy
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
networkx>=3.2
scikit-learn>=1.3

# MELD / MD dependencies
openmm>=8.0      # Require >=8.0 for 'from openmm import unit' namespace
mdtraj
parmed
biopython
mpi4py

# Environment / config
python-dotenv>=1.0
pyyaml>=6.0
azure-identity>=1.17.0
azure-storage-blob>=12.19.0

# Reinforcement Learning dependencies
torch>=2.0
gymnasium>=0.29.0
stable-baselines3>=2.0.0
tensorboard>=2.10.0

# (Optional) meld will be installed in editable mode separately if local repo present
# meld