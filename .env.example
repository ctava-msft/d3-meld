SEQUENCE_FILE=prot_tleap_sequence.dat
PDB_FILE=prot_tleap.pdb
N_REPLICAS=30
N_STEPS=1500
BLOCK_SIZE=50
TIMESTEPS=25000
MINIMIZE_STEPS=20000

# Number of MPI ranks that share each physical GPU (1 leader + workers).
# Example: 4 => 1 leader + 3 workers per GPU.
RANKS_PER_GPU=1

MELD_GIT_REPO=
